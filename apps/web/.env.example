# 호스트 API 서버 주소. 로컬 개발은 빈 값(vite 프록시 사용), 배포 시 Tailscale 주소.
VITE_API_BASE=

# Supabase (로그인·커뮤니티 DB). 값은 대시보드 → Project Settings → API.
# 실제 값은 .env.local(gitignore)에만 넣는다. anon 키는 공개 키(RLS 전제).
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
